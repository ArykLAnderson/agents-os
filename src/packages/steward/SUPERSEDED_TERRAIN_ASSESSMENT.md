# FM-003 WI-031 — Superseded FM-001 terrain assessment

**Original reviewed terrain:** `integration/steward-fm001@5fd6568b23b1b65a0e2de0cf90c4f6292c3d8464` against `origin/master@fd0c6a49cecb218bf60b458201e486599adde2f3`, Case r22, Frame r8, and FM-003. The original bound `feature/steward-space-successor` worktree was clean and allocated at that base before this assessment or production implementation.

**Rebound current-base review:** This assessment is rebound to `origin/master@ded555f4c5562bde22848524e8613360c6dbdb03` and candidate `7ed8b32499e6244b32970b872ff6819f9068ef29`. `git diff --name-status fd0c6a49cecb218bf60b458201e486599adde2f3..ded555f4c5562bde22848524e8613360c6dbdb03` identifies one changed path: `scripts/agents-os.mjs`. The supplied recheck calls that current-base inventory “PR #18”; GitHub’s repository PR listing has no resolvable #18, so the durable pin and file-level inventory here are the authoritative, independently reproducible provenance rather than an invented PR record.

| Generator concern | Current-base model/variant behavior at `ded555f4` | FM-003 generation behavior | Overlap assessment and disposition |
| --- | --- | --- | --- |
| Source inputs | `config/models.json` maps OpenCode tiers to `provider/model:variant`; source agent front matter supplies the selected tier. | `src/packages/steward/**`, including the facade, tests, manifest, and this assessment, is the canonical package source. | **No source-input overlap.** The Steward package neither reads model tiers nor supplies agent front matter. |
| Shared generator seam | `resolveOpenCodeModel` splits only a terminal `:none`, `:low`, `:medium`, `:high`, or `:xhigh` suffix. In the OpenCode arm of `renderAgent`, it emits separate `model` and optional `variant` fields. | `renderPackages` copies the canonical Steward package as a package tree before `renderAgent` runs. | **Shared orchestration, disjoint responsibility.** Both use `scripts/agents-os.mjs`, but model parsing is confined to OpenCode agent rendering and package copying does not inspect or transform model fields. |
| Generated paths | Only `adapters/opencode/generated/agent/**` changes: for example `model: "litellm/openai/gpt-5.6-terra"` with `variant: "medium"`. Pi and Codex agent rendering is outside this change. | `adapters/{pi,codex,opencode}/generated/packages/steward/**` receives the same copied package tree. | **No output-path collision.** OpenCode agent output is under `agent/**`; FM-003 package output is under `packages/steward/**` for all three targets. The ordered `renderTarget` calls cannot overwrite either surface with the other. |
| Consumer proof | OpenCode consumers receive provider/model and reasoning variant as distinct fields, rather than a colon-suffixed model value. | Pi, Codex, and OpenCode consumers receive the same `steward-result@1` package and the declared typed unavailable outcomes when a real owner endpoint is absent. | **Compatible.** Clean generation/install, installed-package tests in all three targets, and the source/generated digest check are required convergence evidence; neither behavior claims a new owner endpoint. |

FM-001 is a Namespace-local, provider-module route. FM-003 is one database-scoped Steward with Space-owned custody and an independently packaged facade. The old source cannot be imported as behavior: its owner identity, storage/module boundary, protocol, and generated/manifest coupling contradict the accepted successor.

| Old slice/file family | File-level dependencies/coupling observed | Disposition | FM-003 consequence |
| --- | --- | --- | --- |
| Namespace admission: `lib/steward/admission.mjs`, `steward-namespace-admission.test.mjs` | `context/namespace.mjs`; requires one non-root Namespace as Steward identity | **discard** | Namespace is Space metadata only; no Namespace-local Steward exists. |
| Provider registration/custody: `lib/steward/custody.mjs`, `steward-provider-module.test.mjs`, `steward-custody.test.mjs`, `steward-custody-restart-proof.test.mjs` | closed provider registry, `operations/modules.mjs`, mechanical successor SQL transaction/envelope and `steward-intake@1`/`steward-matter@1` schemas | **discard** behavior; **pattern-only re-derivation** of immutable Intake, replay digest, CAS, and restart proof | `StewardStore` is the sole F-007 persistence boundary; it exposes no Casebook owner kind, provider module, SQL, or old envelope. |
| Intent/right/owner call: `intent.mjs`, `execution-right.mjs`, `owner-operation.mjs`, `steward-durable-intent.test.mjs` | durable queue, monotonic execution-right fence, owner-operation dependency | **discard** | queues, execution rights, generic owner protocols, and delivery coordination are explicitly excluded. |
| Namespace inventory/broad views: `broad-admission.mjs`, `broad-binding.mjs`, `broad-components.mjs`, `steward-broad-admission.test.mjs`, `steward-broad-components.test.mjs`, inventory tests | Context discovery views, policy/visibility and per-Namespace component authority | **discard** | Portfolio/Search work belongs to F-008 and cannot reuse FM-001 authority or Namespace partitioning. |
| Briefing/finding projection: `briefing.mjs`, `finding-projection.mjs`, `steward-briefing*.test.mjs`, `steward-finding-projection.test.mjs` | copied briefing/attention state and old delivery findings | **quarantine** for F-008 review; no code imported | F-007 retains only custody relevance/return; attention and acknowledgement remain later capability groups. |
| Continuity/session: `continuity.mjs`, `session-port.mjs`, `pi-v1-session-adapter.mjs`, corresponding tests | Pi backend, session bindings, focus and replaced generation semantics | **quarantine** | FM-003 defers GS-014 session switching and cannot introduce its old session contract. |
| Namespace Context/substrate edits: `lib/context/index.mjs`, `namespace.mjs`, `resource/admission-guards.mjs`, `mechanical-successor.mjs`, schema/migrations/runtime manifest | Casebook schema v2, successor migrations, admission records, generated provider manifest/digest | **discard** | FM-003 has no migration, Casebook owner change, provider mutation, or dependency on an old runtime manifest. |
| Casebook CLI edits: `src/packages/casebook-cli/**`, `wi033*.test.mjs`, CLI E2E tests | casebook package assembly and copied provider runtime | **discard** | the dedicated Steward package is independent; Casebook CLI stays an external owner boundary. |
| Old generated assets/manifests: casebook-persistence `manifest.json`, shared manifest/protocol, SQLite runtime manifest | source-to-generated digest chain for the former provider package | **discard** | FM-003 generation starts from `src/packages/steward` and the sole `scripts/agents-os.mjs` path. |
| Secret scan patterns in old custody/mechanical code | pure local structural validation before persistence | **pattern-only re-derivation if needed** | no old protocol or scanner is imported; F-007 persists no credentials or bearer material. |

No stale source, test, schema, manifest, generated output, or commit was cherry-picked. The only permitted reuse is the independently re-derived engineering pattern of process isolation, immutable replay identity, complete state manifests, revision/CAS refusal, atomic replacement, package assembly, and normal source generation. This assessment is a review artifact, not an executable contract; executable F-007 behavior is covered by the public facade suites.
