# Invocation And Re-Exercise Record

- **Attempt:** exercise-02-attempt-01
- **Mode:** local human/agent execution of canonical Markdown instructions
- **External writes:** none
- **Semantic validators:** none

## Actual Invocation

1. Read the existing Case root, source registration record, SNAP-003 manifest, APR-002, and the retained stale SNAP-001 artifact and trace.
2. Applied `compose-document` with the RFC adapter to SNAP-003 and recorded the selected and omitted entries in `selection-manifest.md`.
3. Applied `shape-document` with the decision-brief strategy to produce candidate rev-01.
4. Applied `trace-artifact` with the trace, coverage, visual, and staleness resources to produce `artifact.trace.md`.
5. Applied `review-document` through fidelity, genre, editorial, and isolated fresh-reader lenses.

## Observed Re-Exercise And Refinement

The first candidate incorrectly asked for approval of an already-accepted policy and used an unsupported "normal migration process" assignment. Its table had only a table-level unit, so mutations could not identify an individual material row. Candidate rev-02 removed the process claim but still treated ratification as Case-supported. The revised canonical resources distinguish supplied exercise actions from Case-backed reader actions. Candidate rev-03 is a decision record with no Case-backed reader action; its local review instruction is explicit external context, is not traced as support, and prevents publication readiness.

The re-exercise applied the affected composition, shaping, trace, genre, fidelity, and fresh-reader seams to candidate rev-03. The declared review inputs are candidate rev-03 only, with the Case, snapshots, sources, trace, and other review records excluded as documented in `reviews/fresh-reader.md`. The original Case, snapshot manifests, retained artifact, and retained trace were not edited.
