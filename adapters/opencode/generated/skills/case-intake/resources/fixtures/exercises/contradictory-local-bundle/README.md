# Contradictory Local Bundle Exercise

This fixture is one portable, local-only Case root for a representative `case-intake` followed by `case-reconcile` execution. It is a human-readable worked exercise, not a runnable validator or a claim that the skill performed filesystem mutations itself.

- **Case:** notification-retry-policy
- **Purpose:** establish an approved retry-policy direction for a customer-notification RFC.
- **Reader action:** decide whether the RFC may describe a current retry policy.
- **Exercise date:** 2026-07-15 through 2026-07-16
- **External actions:** none

## Execution Order

1. Register the three initial supplied sources in `source-records.md` before reading semantic candidates.
2. Extract the pre-approval working ledger in `pre-approval-case.md`; retain the stale policy as historical and the transcript proposal as non-binding.
3. Ask the single material question in `author-review-prompt.md` and record the explicit response in `author-response-intake.md`.
4. Create the approved initial Case and immutable `SNAP-001` manifest.
5. Register `sources/later-capacity-evidence.md` in the reconciliation phase, then stop downstream RFC work for the blocking contradiction.
6. Record the author correction, supersession, `SNAP-002`, and the stale-artifact notice in this Case root.
7. Inspect `document-system-exercise-03.md` for the subsequent document-system exercise: a report composed from `SNAP-003`, then a controlled contradiction reconciled into `SNAP-004` without mutating the frozen baseline.

## Invocation

Use `case-intake` with this directory as the supplied local source bundle and declared exercise purpose. Follow `INVOCATION.md` in order; it records the source-registration, extraction, author-review, and snapshot boundaries as behavior for a human or an agent to perform. After `SNAP-001`, use `case-reconcile` with `sources/later-capacity-evidence.md` as new evidence and follow the reconciliation section in the same file.

The reviewer packet is intentionally limited to the Case, sources, author interaction, and reconciliation result. It is evaluation evidence only, is not an approval event, and cannot create authority.
