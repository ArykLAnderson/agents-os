# Contradictory Local Bundle Reconciliation

This is the `case-reconcile` half of the local exercise rooted at `case-intake/resources/fixtures/exercises/contradictory-local-bundle/`.

The initial Case pinned the RFC's current-policy claim to `SNAP-001/DEC-001`. Later capacity evidence conflicts with the accepted three-retry limit. Reconciliation registers that evidence, interrupts immediately, preserves the original decision, applies only the explicit author correction, creates `SNAP-002`, and marks the affected RFC unit stale.
